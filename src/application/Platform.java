package application;

import javafx.scene.paint.Color;

class Platform extends javafx.scene.shape.Rectangle {
public Platform(double x, double y, double width, double height) {
super(x, y, width, height);
setFill(Color.GREEN);
}
}